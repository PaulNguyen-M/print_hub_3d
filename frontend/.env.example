# ─── Frontend environment variables (Print Hub 3D) ───
# Tạo file .env (hoặc .env.local) từ mẫu này. Vite chỉ đọc biến có tiền tố VITE_.

# URL gốc của API backend
VITE_API_BASE_URL=http://localhost:8080/api/v1
